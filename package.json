{
    "name": "tamveridemoweb",
    "version": "1.0.0",
    "description": "Web kontrol paneli - Firebase entegrasyonlu makine yönetim sistemi",
    "main": "index.html",
    "scripts": {
        "dev": "live-server --port=3000 --open=/index.html",
        "build": "node generate-env.js && rm -rf public && mkdir public && mkdir public/css public/js public/assets && cp index.html public/ && cp dashboard.html public/ && cp setup-test-data.html public/ && cp env.js public/ && cp -R css/* public/css/ && cp -R assets/* public/assets/ && cp js/firebase-config.js public/js/ && cp js/auth.js public/js/ && cp js/utils.js public/js/ && cp js/app.js public/js/",
        "start": "live-server --port=8080 --open=/index.html"
    },
    "dependencies": {
        "dotenv": "^16.5.0",
        "firebase": "^10.0.0"
    },
    "devDependencies": {
        "live-server": "^1.2.2"
    },
    "keywords": [
        "firebase",
        "dashboard",
        "web-panel",
        "machine-management"
    ],
    "author": "TamVeriWeb Team",
    "license": "MIT"
}
